formae agent has been deployed!

1. Get the application URL:
{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "formae.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc {{ include "formae.fullname" . }} -w
{{- else }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "formae.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  echo "Visit http://127.0.0.1:{{ .Values.service.port }}"
{{- end }}

2. Datastore: {{ .Values.formae.datastore.type }}
{{- if and (eq .Values.formae.datastore.type "postgres") .Values.postgresql.enabled }}
   PostgreSQL is deployed in-cluster.
{{- end }}

{{- if .Values.grafana.dashboards.enabled }}

3. Grafana dashboards have been provisioned as ConfigMaps.
   They will be auto-discovered by a Grafana sidecar watching for label: {{ .Values.grafana.dashboards.label }}={{ .Values.grafana.dashboards.labelValue }}
{{- end }}

{{- if .Values.otelCollector.enabled }}

4. OpenTelemetry Collector sidecar is running.
   Metrics are exported on port 9090 (Prometheus format).
{{- end }}
