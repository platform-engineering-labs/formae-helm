apiVersion: v1
kind: Service
metadata:
  name: {{ include "formae.fullname" . }}
  labels:
    {{- include "formae.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.otelCollector.enabled }}
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "formae.selectorLabels" . | nindent 4 }}
    ## Without the component label this Service also matches the PostgreSQL
    ## pod, which carries the same name/instance labels — half the API traffic
    ## would be routed to PostgreSQL.
    app.kubernetes.io/component: agent
