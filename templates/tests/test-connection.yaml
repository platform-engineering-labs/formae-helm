apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "formae.fullname" . }}-test-connection"
  labels:
    {{- include "formae.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "formae.fullname" . }}:{{ .Values.service.port }}/api/v1/health']
  restartPolicy: Never
